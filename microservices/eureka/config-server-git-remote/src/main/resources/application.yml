server:
  port: 8775
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig
          searchPaths: respo
          skipSslValidation: true
          username:
          password:
    label: master
  application: config-server